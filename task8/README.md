Задание 8  Найти ошибки.

Вызывается перемменная через ${  } и последней скобкине хватало.

Далее init прошел, и при validate обнаружили следующие ошибки,

["nat_ip_address"] - было не закончено, не хватало ]

с пробелом дальше пришлось повоевать _id["]

далее fqdn=${i.fqdn} указал как было ранее.
и проверил на 
platform_id=${i["platform_id"]}

все заработало.
